# 开发工作记录

> 本文件记录"假设生成 Agent 改进计划"（见 `假设生成Agent改进计划.md`）的实施过程与代码改动。
> 每轮改动按日期分节，包含：改动目标、涉及文件、关键变更、验证结果。

---

## 2026-08-20 · P0：打通 03→04 数据流 + 结构化科学问题

### 改动目标

1. 03 模块新增"确认问题"动作：把对话收敛结果结构化为 JSON 存入 `question_synthesis.sqlite` 新表 `confirmed_questions`。
2. 04 模块优先读取 `confirmed_questions` 作为问题来源（回退到原有拼接逻辑）。
3. 04 每条路线的 evidence 绑定真实 evidence ids，标注"证据支撑 / 推测"。
4. 03 记录研究者输入次数（人类介入成本指标），前端展示。

### 涉及文件与关键变更

| 文件 | 变更 |
|---|---|
| `03_科学问题归纳_agents/src/question_synthesis_agent/db.py` | 新增 `confirmed_questions` 表（problem_statement / variables_json / mechanism_hypothesis / validation_criteria_json / evidence_ids_json / source_message_id / mode / created_at）；新增 `add_confirmed_question()`、`confirmed_questions()`、`human_intervention_count()`、`confirmed_question_to_dict()` |
| `03_科学问题归纳_agents/src/question_synthesis_agent/agent.py` | 新增 `CONFIRM_SYSTEM_PROMPT`（结构化确认提示词）；新增 `confirm()` 方法（LLM 结构化 + 规则兜底 + evidence id 校验 + 确认结果写入对话）；`state()` 增加 `confirmed_questions` 与 `interventions`；新增辅助函数 `collect_evidence_ids` / `parse_confirm_json` / `_extract_json_object` / `_fallback_confirm` / `_filter_evidence_ids` / `_confirm_summary` 等 |
| `03_科学问题归纳_agents/src/question_synthesis_agent/cli.py` | 新增 `confirm` 子命令（支持 `--json`） |
| `04_提出路线/src/route_candidate_agent/agent.py` | 新增 `_confirmed_question_options()`（读取 03 的 sqlite，id 从 20000 起）；`collect_context()` 合并 confirmed 来源；`merge_question_options()` confirmed 优先排序；`build_prompt()` 对确认问题注入 variables / validation_criteria / evidence_ids 并提示引用 evidence id；新增 `collect_evidence_pool()` + `annotate_evidence()`（确定性证据标注），`generate()` 输出 run 增加 `evidence_pool_size`，每条 route 增加 `evidence_annotations` |
| `文献检索_agent/src/lit_agent/question_synthesis_bridge.py` | 新增 `question_synthesis_confirm()` |
| `文献检索_agent/src/lit_agent/web_v3.py` | 新增 POST 动作 `/question-synthesis/confirm`；03 页面"会话操作"面板新增"确认当前科学问题"按钮与研究者输入次数显示；新增"已确认科学问题"面板（`question_confirmed_list`）；04 页面 route_card 的证据依据改为带标注渲染（`route_evidence_block`，绿色"证据支撑" / 黄色"推测"徽章 + matched ids）；`_empty_question_synthesis_state()` 补充新字段；`question_synthesis_css()` 增加 `.confirmed-item` 样式 |

### 设计决策

- **confirmed 问题的 id 从 20000 起**：避免与 01 的 goal id（整数）和 03 临时 id（10000 起）冲突。
- **evidence id 校验**：LLM 返回的 evidence_ids 只保留证据池中真实存在的 id，杜绝幻觉 id。
- **证据标注是确定性的**（字符串包含匹配），不依赖 LLM 自评，规避 LLM-as-Judge 乐观偏差。
- **LLM 失败降级**：confirm 在无 API key / LLM 出错时用规则兜底（问题陈述取最近研究者消息，证据取池中前 8 条），流程不中断。

### 验证结果

| 验证项 | 结果 |
|---|---|
| 03 原有测试（pytest） | 2 passed |
| 01 模块全部测试（含 route_candidates） | 26 passed |
| 04 单测脚本：confirmed 优先排序 | 通过（confirmed 排最前，共 4 个来源合并） |
| 04 单测脚本：证据标注 | 通过（"证据支撑" + matched_ids / "推测"） |
| 03 CLI 端到端（chat → confirm --json，真实 LLM） | 通过：输出结构化问题，evidence_ids 为真实证据（p:32:p00029 等） |
| web_v3.py / bridge 语法检查 | 通过 |
| 03 真实库 schema 升级 | 通过：新增 confirmed_questions 表，原数据保留 |

### 待跟进

- P0 剩余项：04 路线证据在 LLM 路径下的标注依赖 LLM 在 evidence 文本中引用 id（已在 prompt 中要求）；若 LLM 不引用，则标注为"推测"，属预期行为。
- 下一步：P1（激活知识图谱：图上下文注入 + gap_detection + 图新颖性指标）。

---

## 2026-08-20 · P0 收尾 + P1：激活知识图谱

### P0 收尾

1. `build_prompt()` 注入证据 id 池（`evidence_pool`）：LLM 路径下提示词明确给出可引用的真实 evidence ids，配合确定性标注形成闭环。
2. 补充正式测试：03 新增 `test_confirm_*`（LLM 结构化路径 + 兜底路径 + evidence id 过滤）；04 新增 merge / annotate 测试（`tests/test_route_candidates.py`）。

### P1 改动目标

1. 04 新建 `graph.py`：加载 02 模块的 `data/graph/graph.json`，提供确定性图操作。
2. 四种图规划操作：`gap_candidates`（方法×材料未共现 = 研究空白）、`analogy_candidates`（共享属性的方法迁移候选）、`constraint_list`（wiki limitations 已知限制）、`feasibility_map`（语料覆盖/未覆盖实体）。
3. `graph_novelty()` 确定性图新颖性评分（高/中/低 + 理由），集成到 `generate()` 后处理。
4. `graph_context()` 子图上下文（问题匹配实体 + 邻域 + 证据 ids）注入 LLM 提示词。
5. 前端 04 页面：图谱洞察面板（gap/类比/限制/语料未覆盖）+ 每条路线的"图新颖性"徽章 + 图谱节点/边指标。

### 涉及文件与关键变更

| 文件 | 变更 |
|---|---|
| `04_提出路线/src/route_candidate_agent/graph.py`（新建） | `KnowledgeGraph`（图加载 + 邻接表 + BFS 最短路径 + 边标签索引）；`graph_stats` / `graph_context` / `gap_candidates` / `analogy_candidates` / `constraint_list` / `feasibility_map` / `graph_novelty` / `load_knowledge_graph`；节点类型：paper / material / method / property / wiki_topic / claim / evidence；边类型：mentions / uses_method / has_property / supports |
| `04_提出路线/src/route_candidate_agent/config.py` | 新增 `default_analysis_agent_dir()` 与 `default_graph_path()` |
| `04_提出路线/src/route_candidate_agent/agent.py` | `collect_context()` 增加 `graph` 块（stats / context / gap_candidates / analogy_candidates / constraints / feasibility）；`build_prompt()` 经 `_graph_prompt_payload()` 注入图上下文（匹配实体 / 邻域节点 / 证据 ids / gap / 类比 / 限制）；`generate()` 后处理 `annotate_graph_novelty()`，run 增加 `graph` 字段；新增 `annotate_graph_novelty()` |
| `文献检索_agent/src/lit_agent/web_v3.py` | 04 页 metrics 增加"图谱节点 / 图谱边"；侧栏新增"图谱洞察"面板（`route_graph_insights()`）；`route_card()` 增加"图新颖性"徽章（高/中/低，title 显示评分理由）；`route_candidate_css()` 新增 `.graph-insight` 样式；`_empty_route_candidate_state()` 补充 graph 块 |
| `文献检索_agent/tests/test_route_graph.py`（新建） | 合成图测试：gap 候选（未共现方法×材料）、类比候选（共享属性）、novelty 评分（高/中/低）、constraint 过滤、feasibility 未知实体 |

### 设计决策

- **图指标全部确定性计算**（邻接表 + BFS + 字符串匹配），不依赖 LLM，与证据标注同一原则。
- **gap 定义**：方法在图中被使用过、材料在图中有记录，但两者在任意论文边中共现次数为 0 → 空白候选。
- **类比定义**：两个材料共享 ≥1 个属性且方法只在一侧使用过 → 方法迁移候选。
- **新颖性分级**：路线实体对全部在图中共现过 → 低；部分共现 → 中；存在未共现对或仅单一类实体 → 高。
- **constraints 过滤**：只保留 ≤300 字的 limitations 短句（02 模块可能把论文摘要误存入 limitations）。
- **LLM 提示词只注入图结构摘要**（最多 24 邻域节点 / 8 gap / 6 类比 / 8 限制），控制 token 成本。

### 验证结果

| 验证项 | 结果 |
|---|---|
| 文献检索_agent 全量测试（含新 test_route_graph.py） | 33 passed |
| 03 模块测试（含新 confirm 测试） | 5 passed |
| 语法检查（agent.py / graph.py / config.py / web_v3.py） | 通过 |
| 真实图端到端：state() 图块 | 通过：35 节点 / 34 边 / 6 材料 / 3 方法 / 4 属性 / 1 论文；constraints 1 条；feasibility 识别 2 个语料未覆盖实体 |
| 真实图端到端：generate() 本地草稿模式 | 通过：run 含 graph stats；每条 route 含 `graph_novelty`（level + reason + matched_entities）与 `evidence_annotations`（全部命中证据支撑） |
| 网页渲染 render_route_candidates() | 通过：图谱洞察面板 / 图谱节点与边指标 / 图新颖性徽章 / graph-insight 样式 / 证据徽章全部存在；空状态含 graph 块 |

### 待跟进

- 真实图目前只有 1 篇论文入库，gap / 类比候选为 0 属预期；随着 02 模块构建更多 Paper Cards，候选会自然涌现（合成图测试已验证逻辑）。
- 下一步：P2（Critic 批判者 + Elo Arena 路线质量评分）。

---

## 2026-08-20 · P2：Critic 批判者 + Elo Arena + 一轮演化

### 改动目标

1. 新增 `CriticAgent`（critic.py）：对每条路线生成结构化四维评分（新颖性 / 可行性 / 证据充分性 / 可证伪性，1-5 + 理由）+ 主要弱点 + 改进建议。
2. **关键设计：批判依据 = 生成上下文快照**。`generate()` 把生成时使用的完整上下文（结构化问题字段 / 证据池 / 图摘要）持久化为 `run["snapshot"]`，Critic、Arena、演化全部只读该快照，杜绝"批判者看不到生成时上下文"的问题。
3. Elo Arena：相邻对两两比较 + Elo 排名（初始分由确定性四维总分映射，LLM 可用时仲裁复核，K=32），替代绝对打分。
4. 一轮演化：`evolve()` 基于批判意见生成 v2 路线，`lineage` 字段保留 parent → child 谱系。
5. 前端：每条路线四维评分徽章 + Elo 徽章 + 批判意见块；Critic Arena 面板；批判评审 / 一轮演化按钮；谱系展示。

### 涉及文件与关键变更

| 文件 | 变更 |
|---|---|
| `04_提出路线/src/route_candidate_agent/critic.py`（新建） | `CRITIC_SYSTEM_PROMPT`（评判必须落在上下文快照上）；`build_critique_prompt` / `build_evolve_prompt`；`fallback_critique`（确定性四维评分：证据支撑占比与 id 数、定量验证判据、图新颖性、constraints/语料覆盖冲突）；`critique_routes`（LLM + 归一化 + 漏项兜底补齐）；`elo_arena`（相邻对 + K=32 + LLM 仲裁）；`fallback_evolve`（本地修订 v2）；`_constraint_hit` / `_term_hit`（中英文限制匹配） |
| `04_提出路线/src/route_candidate_agent/agent.py` | `generate()` 增加 `with_critique` 参数与 `snapshot` 持久化；新增 `_apply_critique()`（批判 + arena 写回 routes 的 critique/elo 字段与 run 的 critique 块）；新增 `critique()` / `evolve()` / `_save_runs()` 方法；新增 `_find_run()` / `_rebuild_snapshot()`（旧 run 无快照时重建并标记 rebuilt） |
| `文献检索_agent/src/lit_agent/route_candidate_bridge.py` | 新增 `route_candidate_critique()` / `route_candidate_evolve()` |
| `文献检索_agent/src/lit_agent/web_v3.py` | 新增 POST 动作 `/route-candidates/critique`、`/route-candidates/evolve`；生成按钮改为"生成候选路线（含批判评审）"；新增批判评审 / 一轮演化按钮；`route_card()` 增加 Elo 徽章与 `route_critique_block()`（四维评分徽章 + 批判弱点 + 改进建议）；新增 `route_arena_block()`（Elo 排名 + 两两对决记录）；候选路线下新增 Critic Arena 面板；`route_run_meta()` 展示演化谱系；CSS 新增 route-actions / critique-dims / critique-weak / critique-imp / arena-battles 样式 |
| `文献检索_agent/tests/test_route_critic.py`（新建） | 9 个测试：fallback 四维评分、Elo 确定性排名、无 LLM 走兜底、fallback_evolve v2 语义、_apply_critique 写回、generate 持久化 snapshot、with_critique=False 跳过、critique/evolve POST 动作、Arena 面板渲染 |

### 设计决策

- **快照是评判的唯一依据**：`run["snapshot"]` 与注入生成提示词的 `_graph_prompt_payload` 完全一致（生成器看到什么，批判者就评判什么）。
- **旧 run 兼容**：历史 run 无 snapshot 时用当前上下文重建并标记 `rebuilt: true`（提示词中告知 Critic 快照为重建）。
- **LLM 输出归一化**：四维缺一即丢弃该条；漏掉的路线用确定性兜底补齐；Arena 仲裁失败回退确定性裁决。
- **Elo 而非绝对分**：初始分 1000 + 40 × (总分 − 12)；只对总分排序后的相邻对仲裁（n−1 场），控制 LLM 调用成本。
- **确定性兜底全部基于可审计信号**：证据支撑占比与 matched ids 数量、验证文本中的量化标记、图新颖性等级、constraints / 语料未覆盖命中。

### 验证结果

| 验证项 | 结果 |
|---|---|
| 文献检索_agent 全量测试（含新 test_route_critic.py 9 个） | 42 passed |
| 03 模块测试（回归） | 5 passed |
| 语法检查（agent.py / critic.py / bridge / web_v3.py） | 通过 |
| 真实数据端到端：generate 含批判 | 通过：snapshot 含 question/evidence_pool/graph；critique mode=fallback；Elo 排名 1→2→3（1296/1280/1184） |
| 真实数据端到端：evolve 一轮演化 | 通过：v2· 标题前缀、改进建议并入、lineage 含 parent_run_id 与 critique_mode |
| 网页渲染 | 通过：Critic 评审块 / Critic Arena 面板 / Elo 排名 / 批判评审与一轮演化按钮 / 谱系展示全部存在 |
| LLM 输出归一化（合成输入） | 通过：合法条目归一化、非法 rank 丢弃、坏输出抛 ValueError 走兜底；critique/evolve 提示词均含快照证据与路线全文 |

### 待跟进

- LLM 路径（critique llm / arena llm 仲裁 / evolve_llm）代码就绪但当前 CLI 环境无 API key 未实跑，下次在 web 服务（apply_active_llm_to_env）或配 key 环境验证。
- 批判评分与 Elo 已写入 `route_candidates.json`，可作为 P4 回溯评估的双维专家盲评素材。
- 下一步：P3（假设去重与多样性：proximity 检查 + 显式多样性约束，与 P2 共享比较器）。

---

## 2026-08-20 · P2 修正：评判通道分离（防幻觉传播）

### 背景与依据

用户反馈：批判者看到生成假设时的上下文会带来幻觉，并指出 REPORT/2605.23204v1.pdf 中有相关论述。论文 §6.4 Reliability 明确指出：多阶段研究工作流中，幻觉（hallucinated citation、误读等）会**被中间输出吸收并被下游阶段复用**（"absorbed into intermediate outputs and reused by later stages... a hallucinated citation may distort the literature context"）；§6.3 警告批判层可能"reproducing the appearance of scientific judgment without fully capturing it"；Novelty 评估须独立于生成时的检索基础。

因此推翻 P2 初版设计"批判依据 = 生成上下文快照"——快照中的 evidence_pool、图倾向性摘要（gap/analogy 候选）、机制假设本身就是生成器的输入，其中若含幻觉（幻觉引用/误读/臆造的机制假设），Critic 会将其当真相复用，形成幻觉自我强化。

### 修正内容

- **批判者唯一证据依据 = 独立检索证据**：新增 `independent_evidence_search()`，基于路线自身内容（candidates / variables / title，而非生成器选择的证据池）在 02 语料（Paper Cards / Wiki）重新检索证据；`build_critique_prompt()` 不再注入生成器的证据池、图倾向性摘要、03 预设机制假设/变量/证据，只给：研究问题标题+描述（评判对象）、路线自身内容（标注为"声称，待检验"）、独立检索证据块、02 语料客观已知限制（wiki limitations，非生成器产出）。
- **声称引用交叉验证**：`fallback_critique()` 新增"声称 vs 独立检索"核对——生成器声称引用的 id 若独立检索无法复现，证据分降级为 ≤2 并记录"幻觉风险"弱点；可复现则不降级。
- **Arena 仲裁同步改造**：`_llm_compare()` 只依据独立检索证据对比两条路线。
- **快照降级为审计用途**：`run["snapshot"]` 不再进入批判提示词，仅供演化（生成器侧延续）与人工审计；`_question_text()` 不给 03 的 variables / validation_criteria / mechanism_hypothesis / evidence_ids。
- **前端**：Critic Arena 面板徽章改为"独立证据检索 · 防幻觉传播"；批判按钮 busy 文案同步；每条路线展示"独立检索：检索词 → 命中 N 条证据来源"。

### 验证结果

| 验证项 | 结果 |
|---|---|
| 文献检索_agent 全量测试（更新断言 + 新增交叉验证两向性测试） | 43 passed |
| 03 模块测试（回归） | 5 passed |
| 真实数据端到端：独立检索 | 通过：corpus_size=14；每条路线命中 4-6 条证据来源；claim 复现 ids 写入 independent_verification |
| 批判提示词泄漏检查 | 通过：evidence_pool / 机制假设 / analogy / gap 候选全部不出现于批判提示词 |
| 交叉验证两向性（合成输入） | 通过：无独立检索时声称引用降级为 2 分 + 幻觉风险弱点；独立检索复现时恢复原分 |

---

## 子 agents Web UI 优化（仅页面专属样式，首页不受影响）

改动全部位于 `文献检索_agent/src/lit_agent/web_v3.py`，不修改共享 `base_css()`：

- **模块视觉身份**：每个 stage 页面定义 `--stage/--stage-deep/--stage-soft` 主题色令牌（01 蓝 #1e40af / 02 青 #0891b2 / 03 靓蓝 #4f46e5 / 04 绿 #16a34a）；02 页新增 `paper_analysis_css()` 与青色 `paper-hero`；03 页 hero 从通用蓝改为靓蓝渐变；hero counters 边框色跟随主题。
- **信息层级与密度**：修复 04 页 8 张 metric 卡在 6 列栅格下的失衡（改为 4×2，含响应式降级）；02 页 metrics 改为 auto-fit 自适应。
- **数据可视化**：Critic 四维评分从文字徽章改为彩色进度条（score-high/mid/low 三档渐变，宽度=分数×20%）；`metric_card()` 新增可选 `ratio` 参数渲染真实进度条（01 页“已获 PDF/本轮候选”、02 页“已转换/PDF 队列”），首页未使用该参数，行为不变。
- **交互与空状态**：聊天气泡区分角色（用户🧑‍🔬/AI🤖/检索🔎 图标 + 不对称圆角 + 渐变底）；`.route-empty`/`.graph-empty` 空状态新增图标；metric / route-card / paper-card / wiki-item / rag-result / graph-insight / context-card / 气泡 / suggestion 按钮均增加 hover 提升动效（遵循 prefers-reduced-motion）。

验证：`py_compile` 通过；全量测试 43 passed；五个页面渲染冒烟测试通过；服务重启后 curl 抽查四页新样式标记均命中、首页无新样式泄漏；真实 critique 数据下评分条输出正确（100%/80% 宽度）。

---

## 外部 LLM API key 配置完成（Sub2API 中转站）

- **排查过程**：用户提供的两个 key（grok / gpt）在 `https://nimabo.io/v1` 一律返回 `INVALID_API_KEY`；经探测发现 nimabo.io 域名只是 Sub2API 中转站前台网页，真实 API 网关为 **`http://95.169.21.86:8080/v1`**（HTTP 协议 + 8080 端口，80/443 均拒绝连接，HTTPS 报 WRONG_VERSION_NUMBER）。
- **验证结果**：两个 key 均通过鉴权——grok key 可见 134 个模型（含 `grok-4.5`），gpt key 可见 20 个模型（含 `gpt-5.6-terra`）；`/v1/chat/completions` 实测两模型均正常返回，`content` 字段非空（grok-4.5 附带 reasoning_content 但不影响解析）。
- **配置更新**：`configs/external_llm_apis.json` 两个 API 的 base_url 均改为 `http://95.169.21.86:8080/v1`（nimabo GPT `gpt-5.6-terra` 启用为主，nimabo Grok `grok-4.5` 备用）；`configs/local.env` 同步切换。两文件均在 .gitignore 中，key 不入库。
- **端到端验证**：项目自身 `OpenAICompatibleClient.chat_json()`（含 `response_format: json_object`）经项目 http 层调用成功，返回解析正常。
- **意义**：LLM 实链路已打通，03 确认、04 生成/批判/演化的 LLM 路径可实跑；原 deepseek-v4-pro 推理模型 content 为空的兼容问题不再阻塞（新模型 content 正常）。

---

## LLM 全链路实跑验证（01/03/04 模块）+ 推理模型兼容加固

### 1. 推理模型兼容加固（四处统一）

- 01 `lit_agent/llm.py`、04 `route_candidate_agent/agent.py`、04 `route_candidate_agent/critic.py`（JSON 客户端）：新增 `_message_text()`（content 为空时回退 reasoning_content，兼容列表分段 content）与 `_loads_json()`（严格解析失败时从文本中提取最后一个平衡 JSON 对象，兼容代码块包裹/推理内容）。
- 03 `question_synthesis_agent/llm.py`（纯文本客户端）：content 为空时回退 reasoning_content。
- 验证：文献检索_agent 43 passed + 03 模块 5 passed，无回归。

### 2. 独立证据检索对 LLM 长句输出的鲁棒性修复（04 critic.py）

- **问题**：LLM 生成的路线 candidates/variables 是整句描述（如“低Nd含量Nd-Fe-B烧结磁体或热变形磁体…”），原子串匹配在 02 语料上必然 0 命中，导致 Critic 把所有路线判为“无证据支撑”全 1 分（防幻觉机制被长句 bug 误触发）。
- **修复**：查询改用检索原子匹配——英文/材料 token（≥3 字符，归一化连字符，Nd-Fe-B≈NdFeB）任一命中，或中文 3-gram（顿号/逗号分块后滑窗）命中 ≥2 判定命中；queries 字段保持原句供展示。
- **效果**：修复前 3 条路线 0 命中全 1 分；修复后路线一/二各命中 NdFeB wiki 证据页，falsifiability 恢复 3-4 分；路线三（探索性发现路线）仍 0 命中，属正确判定。

### 3. 全链路实跑结果（gpt-5.6-terra）

| 模块 | 路径 | 结果 |
|---|---|---|
| 03 | 对话 ×2 + 确认 | 全走 LLM；确认产出结构化问题（mode=llm）：problem_statement（晶界扩散与主相微观结构耦合调控矫顽力）、8 个 variables、验证标准含 DFT/微磁/XRD-TEM |
| 04 | 生成→独立检索→批判→Arena→演化 | 全走 LLM（mode=llm）；Arena 两场均为 LLM 仲裁（裁决理由基于独立检索证据）；演化两层（v2、v2·v2）均 evolve_llm |
| 01 | 边界相关性评审 | 3 条边界候选全部合理：排除压电能量收集（0.93）、纳入 Tm 替换 2:17 磁体（0.98）、模糊标题判 uncertain（0.7） |

- 演化质量：v2 路线针对 weaknesses 逐条改进，Elo 排序由批判+仲裁驱动（856/800/744）。
- 防幻觉机制实测有效：声称引用 p:32:p00029 等只有在独立检索复现时才被采信；探索性路线因无证据被严格降分。

### 4. 其他

- 说明：Arena 结果字段名为 `arena_mode`，验证脚本最初误读为 `mode` 导致显示 None，实际 Arena 一直正常工作（两场均为 LLM 仲裁）。
- Web 服务已重启加载新代码（127.0.0.1:8765）。
- 临时验证脚本已清理。


