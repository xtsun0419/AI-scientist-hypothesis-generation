[project]
name = "research-quality-layer"
version = "0.1.0"
description = "Opt-in research-quality sidecar: opportunity gate, recon protocol, Idea Pack contracts"
requires-python = ">=3.12"
dependencies = [
  "pydantic>=2,<3",
]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["python"]
